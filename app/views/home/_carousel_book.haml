.item{ class: ( active ? 'active' : '' ) }
  .row
    .col-sm-5
      .general-img-wrap
        = image_tag(book.root_image, class: 'img-shadow general-thumbnail-img')
    .col-sm-7
      .mb-60
        %h1= book.title
        - unless book.description.empty?
          %p.lead= book.description
        %p.lead.small= book.authors
        .general-align
          = link_to cart_item_path(items: {price: book.price, quantity: 1, book_id: book.id}), method: :post do
            %button{class: (book.quantity.zero? ? 'disabled btn' : 'btn btn-primary'), disabled: book.quantity.zero? }= 'Buy Now'